
spring.cloud.gcp.sql.database-name=sproutCSCI201
spring.cloud.gcp.sql.instance-connection-name=cscifinal:us-west3:sprouter

# So app starts despite "table already exists" errors.
spring.datasource.continue-on-error=true
# Enforces database initialization
spring.datasource.initialization-mode=always

# Leave empty for root, uncomment and fill out if you specified a user
spring.datasource.username=root

# Uncomment if root password is specified
spring.datasource.password=root

#spring.cloud.gcp.project-id=
spring.cloud.gcp.credentials.location=file:./cscifinal-1c86f15a1691.json
